<?php

namespace Raccoon\Resource\Lang {

    /**
     * LOCALIZATION
     * -----------------------------------------------
     * Define translations for your text resources.  
     */

    Locale::id('message.code.0')->en('Success!!!'); 
    Locale::id('message.code.1')->en('Do not left the ${name} field empty.');
    Locale::id('message.code.2')->en('${name} must have at least ${length} character${plural} in length.');
    Locale::id('message.code.3')->en('${name} must not exceed ${length} character${plural} in length.');
    Locale::id('message.code.4')->en('${name} must have at least ${length} word${plural}.');
    Locale::id('message.code.5')->en('${name} must not exceed ${length} word${plural}.');
    Locale::id('message.code.6')->en('${name} must have at least ${length} line${plural}.');
    Locale::id('message.code.7')->en('${name} must not exceed ${length} line${plural}.');
    Locale::id('message.code.8')->en('${name} must contain at least ${length} digit${plural}.');
    Locale::id('message.code.9')->en('${name} must not exceed more than ${length} digit${plural}.');
    Locale::id('message.code.9b')->en('${name} must not contain digits.');
    Locale::id('message.code.10')->en('${name} must contain at least ${length} letter${plural}.');
    Locale::id('message.code.11')->en('${name} must not contain more than ${length} letter${plural}.');
    Locale::id('message.code.11b')->en('${name} must not contain letters.');
    Locale::id('message.code.12')->en('${name} must contain at least ${length} uppercase letter${plural}.');
    Locale::id('message.code.13')->en('${name} must not exceed more than ${length} uppercase letter${plural}.');
    Locale::id('message.code.13b')->en('${name} must not contain uppercase letters.');
    Locale::id('message.code.14')->en('${name} must contain at least ${length} lowercase letter${plural}.');
    Locale::id('message.code.15')->en('${name} must not exceed more than ${length} lowercase letter${plural}.');
    Locale::id('message.code.15b')->en('${name} must not contain lowercase letters.');
    Locale::id('message.code.16')->en('${name} must contain at least ${length} special character${plural}.');
    Locale::id('message.code.17')->en('${name} must not exceed more than ${length} special character${plural}.');
    Locale::id('message.code.17b')->en('${name} must not contain special characters.');
    Locale::id('message.code.18')->en('${name} contains prohibited words or characters.');
    Locale::id('message.code.19')->en('${name} has invalid format.');
    Locale::id('message.code.20')->en('${name} has invalid format.');
    Locale::id('message.code.21')->en('${name} has invalid value.');
    Locale::id('message.code.22')->en('${name} has invalid value.');
    Locale::id('message.code.23')->en('${name} has invalid value.');
    Locale::id('message.code.24')->en('${name} has invalid format.');
    Locale::id('message.code.25')->en('${name} must be a valid numeric value.');
    Locale::id('message.code.26')->en('${name} must have a value more than or equal ${value}.');
    Locale::id('message.code.27')->en('${name} must have a value less than or equal ${value}.');
    Locale::id('message.strength.weak')->en('${name} strength is too weak.');
    Locale::id('message.default')->en('Something went wrong on your request.');
}